
/*
var a = 8;
var result = (a >= 0) ? (a == 0 ? "zero" : "positive") : "negative";
console.log(`The number is ${result}.`);

*/

/*
let age = 20;
let message;

message = age >= 10 ? 'You can drive.' : 'You cannot drive.';

console.log(message);
*/

/*
let speed = 90;
let message = speed >= 120 ? 'Too Fast' : speed >= 80 ? 'Fast' : 'OK';

console.log(message);
*/